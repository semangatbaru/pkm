<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.staydev.seani">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Seani">
        <activity android:name="com.staydev.seani.Chat"></activity>
        <activity android:name="com.staydev.seani.Kritik" />
        <activity
            android:name="com.staydev.seani.Beranda"
            android:label="@string/title_activity_beranda" />
        <activity android:name="com.staydev.seani.RegisterActivity" />
        <activity android:name="com.staydev.seani.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>